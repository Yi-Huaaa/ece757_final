#pragma once
#include <vector>

// partition runtimes for matrix_size = 8
const std::vector<int> matrix8_runtimes = { 8996, 8816, 9092, 8891, };

// partition runtimes for matrix_size = 16
const std::vector<int> matrix16_runtimes = { 54032, 53305, };

